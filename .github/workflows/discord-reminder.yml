name: Discord Eintrage-Erinnerung

on:
  schedule:
    # Täglich um 17:43 UTC (~19-20 Uhr deutsche Zeit, je nach Sommer-/Winterzeit).
    # Bewusst KEINE volle Stunde (z.B. 18:00) - zu diesen Zeitpunkten feuern weltweit
    # extrem viele geplante GitHub-Actions-Jobs gleichzeitig, was laut GitHub zu
    # Verzögerungen von teils mehreren Stunden führen kann. Eine "krumme" Minute
    # reduziert dieses Risiko deutlich (Garantie gibt's bei schedule-Jobs aber nie).
    # Läuft nur, solange noch kein Renntermin bestätigt ist (siehe Skript-Logik) -
    # danach passiert von selbst nichts mehr, kein Spam.
    - cron: '43 17 * * *'
  workflow_dispatch: {} # erlaubt manuelles Auslösen über den "Run workflow"-Button zum Testen

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Repo auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Reminder-Skript ausführen
        env:
          FIREBASE_DB_URL: ${{ secrets.FIREBASE_DB_URL }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_ANNOUNCE_CHANNEL_ID: ${{ secrets.DISCORD_ANNOUNCE_CHANNEL_ID }}
          # Optional: WEBSITE_URL: ${{ secrets.WEBSITE_URL }}
        run: node discord-reminder-bot.js
